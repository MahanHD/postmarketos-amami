# The journal had grown to 617MB on a phone, and it is written continuously:
# journald mmaps its files, so the traffic never shows up as a process' write_bytes
# and is easy to miss when hunting idle power.
[Journal]
SystemMaxUse=64M
SystemMaxFileSize=8M
